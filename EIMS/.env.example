# ==========================================
# FILE CẤU HÌNH MÔI TRƯỜNG MẪU (TEMPLATE)
# Hướng dẫn: 
# 1. Copy file này và đổi tên thành ".env"
# 2. Điền giá trị thật của bạn vào các mục bên dưới
# ==========================================

# --- 1. APPLICATION ENVIRONMENT ---
# Môi trường chạy (Development hoặc Production)
ASPNETCORE_ENVIRONMENT=Development

# --- 2. DATABASE CONFIGURATION (PostgreSQL) ---
# Nếu chạy Docker Compose: DB_HOST=db
# Nếu chạy Local (Visual Studio): DB_HOST=localhost
DB_HOST=db
DB_NAME=EIMS_DB
DB_USER=postgres
# Mật khẩu database (Bắt buộc thay đổi khi deploy thật)
DB_PASS=12345

# --- 3. SECURITY (JWT) ---
# Chuỗi bí mật dùng để ký Token. 
# QUAN TRỌNG: Phải dài ít nhất 32 ký tự để đảm bảo an toàn.
JWT_SECRET=Thay_Doi_Chuoi_Nay_Thanh_Cai_Gi_Do_Dai_Va_Bao_Mat_Hon_123

# --- 4. EMAIL SERVICE (SendGrid) ---
# API Key lấy từ SendGrid Dashboard (Bắt đầu bằng SG.)
SENDGRID_KEY=SG.your_sendgrid_api_key_here
# Email người gửi (Phải verify trên SendGrid trước)
SENDGRID_FROM_EMAIL=no-reply@eims-system.com
# Tên hiển thị người gửi
SENDGRID_FROM_NAME="EIMS System"

# --- 5. FILE STORAGE (Cloudinary) ---
# Thông tin lấy từ Cloudinary Dashboard
CLOUDINARY_NAME=your_cloud_name
CLOUDINARY_KEY=your_api_key
CLOUDINARY_SECRET=your_api_secret

# --- 6. OPTIONAL SETTINGS ---
# Cổng chạy ứng dụng (Mặc định 8000 nếu dùng docker-compose mẫu)
PORT=8000